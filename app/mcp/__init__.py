"""MCP (Model Context Protocol) integration for DealMind.

Provides standardized tool interfaces for proposal editing and Gmail operations.
Tools follow the MCP specification and can be used both in-process (via the chat
endpoint's tool-calling loop) and externally (via the standalone FastMCP server).
"""
